import { TransformHourPipe } from './transform-hour.pipe';

describe('TransformHourPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformHourPipe();
    expect(pipe).toBeTruthy();
  });
});
