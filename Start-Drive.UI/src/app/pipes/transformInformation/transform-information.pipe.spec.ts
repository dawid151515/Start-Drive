import { TransformInformationPipe } from './transform-information.pipe';

describe('TransformInformationPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformInformationPipe();
    expect(pipe).toBeTruthy();
  });
});
