import { TransformLengthDisplayedTextPipe } from './transform-length-displayed-text.pipe';

describe('TransformLengthDisplayedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformLengthDisplayedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
