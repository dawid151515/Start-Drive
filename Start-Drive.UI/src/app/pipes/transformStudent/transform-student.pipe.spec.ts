import { TransformStudentPipe } from './transform-student.pipe';

describe('TransformStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
