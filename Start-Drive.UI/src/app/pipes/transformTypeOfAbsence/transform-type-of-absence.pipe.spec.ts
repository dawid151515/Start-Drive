import { TransformTypeOfAbsencePipe } from './transform-type-of-absence.pipe';

describe('TransformTypeOfAbsencePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTypeOfAbsencePipe();
    expect(pipe).toBeTruthy();
  });
});
